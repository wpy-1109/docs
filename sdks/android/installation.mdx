---
title: "Installation - Android SDK"
sidebarTitle: "Installation"
description: "Install the Alibaba Cloud OSS SDK for Android."
"og:title": "Install OSS Android SDK"
---

# Installation

## Requirements

- Android 4.0 (API level 14) or later
- Android Studio

## Install via Gradle

Add to your `build.gradle`:

```groovy title="build.gradle"
dependencies {
    implementation 'com.aliyun.dpa:oss-android-sdk:2.9.18'
}
```

## Add permissions

Add to `AndroidManifest.xml`:

```xml title="AndroidManifest.xml"
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
```

## ProGuard configuration

If using ProGuard, add:

```text title="proguard-rules.pro"
-keep class com.alibaba.sdk.android.oss.** { *; }
-dontwarn okio.**
-dontwarn org.apache.commons.codec.binary.**
```

<Warning>
  Mobile devices are untrusted environments. Never embed long-term AccessKey credentials in your app. Use STS temporary credentials instead.
</Warning>

## Next steps

<CardGroup cols={2}>
  <Card title="Initialization" icon="gear" href="/sdks/android/initialization">
    Configure the OSS client with STS credentials.
  </Card>
  <Card title="Quick start" icon="rocket" href="/sdks/android/quick-start">
    Upload and download your first object.
  </Card>
</CardGroup>
